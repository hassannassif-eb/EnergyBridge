import SectionHost from '../engine/SectionHost.jsx';

/** VLAN Tracking — VLAN groups, conflicts, CDN tags, bulk edit. */
export default function VlanTracking() {
  return <SectionHost page="vlan" />;
}
