import SectionHost from '../engine/SectionHost.jsx';

/** WAN Solutions — grouped WAN links with pagination and client grouping. */
export default function WanSolutions() {
  return <SectionHost page="wan" />;
}
